version: "3.8"

services:
  db:
    image: postgres:16-alpine
    container_name: devops-portal-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: devops
    volumes:
      - pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d devops"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pg-data:
